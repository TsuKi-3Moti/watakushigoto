<div class="container px-5 px-sm-0">
  <div class="row">

    <div class="col-md-3">
      <div class="d-flex flex-column justify-content-center">

        <h5 class="lead font-weight-bold">管理者</h5>

        <div>
          ・questionの削除<br />
          ・answerの削除<br />
          ・タグの削除<br />
          ・ユーザー情報の編集・削除<br />
            が可能です。
        </div>

      </div>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>ユーザ一覧</h2>
      <table class="table">

        <thead>
          <tr>
            <th>ID</th>
            <th>名前</th>
            <th>メールアドレス</th>
            <th></th>
          </tr>
        </thead>

        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td><%= user.id %></td>

              <td>
                <%= link_to admin_user_path(user) do %>
                  <%= image_tag user.get_profile_image(80, 80), class: "rounded-circle" %><br />
                  <%= user.name %>
                <% end %>
              </td>

              <td><%= user.email %></td>

              <!--ユーザー情報編集のリンク-->
              <td>
                <%= link_to edit_admin_user_path(user), class: "btn btn-outline-secondary rounded-pill" do %>
                  </i><i class="fa-solid fa-user-gear"></i>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>

      </table>
    </div>

  </div>
</div>